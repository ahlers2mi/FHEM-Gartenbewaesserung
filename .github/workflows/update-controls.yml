name: Update controls_Gartenbewaesserung.txt

on:
  push:
    paths:
      - 'FHEM/98_Gartenbewaesserung.pm'
  workflow_dispatch:

jobs:
  update-controls:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Update controls_Gartenbewaesserung.txt
        run: |
          SIZE=$(wc -c < FHEM/98_Gartenbewaesserung.pm)
          DATE=$(date -u +"%Y-%m-%d_%H:%M:%S")
          echo "UPD ${DATE} ${SIZE} FHEM/98_Gartenbewaesserung.pm" > controls_Gartenbewaesserung.txt

      - name: Commit updated controls_Gartenbewaesserung.txt
        run: |
          SIZE=$(wc -c < FHEM/98_Gartenbewaesserung.pm)
          DATE=$(date -u +"%Y-%m-%d_%H:%M:%S")
          echo "UPD ${DATE} ${SIZE} FHEM/98_Gartenbewaesserung.pm" > controls_Gartenbewaesserung.txt

      - name: Commit updated controls_Gartenbewaesserung.txt
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add controls_Gartenbewaesserung.txt
          git diff --cached --quiet || git commit -m "Auto-update controls_Gartenbewaesserung.txt"
          git push
